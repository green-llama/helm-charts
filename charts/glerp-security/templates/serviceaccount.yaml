apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "glerp-security.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "glerp-security.labels" . | nindent 4 }}
    app.kubernetes.io/component: kube-bench
